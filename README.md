# Naive-RNN-Language-Model
My first try on training a NLP-related model.

The task is to build a language model via RNN (technically it is a LSTM) trained on some small txt files(~3MB).

The outcome looks very well.
